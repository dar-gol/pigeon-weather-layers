# Security

Please report vulnerabilities privately to the repository owner instead of
opening a public issue. Do not include access tokens, signed asset URLs,
production manifests, or user locations in a report.

HTTP sources allow assets from the manifest origin by default. Applications
using a separate CDN must explicitly allow its origin.
